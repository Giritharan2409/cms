# Frontend environment variables (optional)
# VITE_API_BASE_URL=http://127.0.0.1:5000

# Backend uses backend/.env (copy backend/.env.example to backend/.env)

# Backend environment variables (set in backend/.env)
# MONGODB_TLS_INSECURE=false  # Set to "true" only for local dev to skip TLS cert verification
